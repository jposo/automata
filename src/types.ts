export enum Mode {
  Create = "create",
  Move = "move",
}

export enum Action {
  AddState = "addState",
  AddTransition = "addTransition",
  DeleteState = "deleteState",
  DeleteTransition = "deleteTransition",
  MoveState = "moveState",
}